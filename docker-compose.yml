version: "3.9"

services:
  db:
    image: postgres:18-alpine
    container_name: sig-operafr-db
    restart: unless-stopped
    networks:
     - sig-operafr-network
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${POSTGRES_HOST_PORT}:${POSTGRES_PORT}"
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  operafr-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sig-operafr-app
    networks:
     - sig-shared-services
     - sig-operafr-network
    working_dir: /app
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - .env
    ports:
      - "2111:2111"
      - "5555:5555"
      - "5556:5556"
    volumes:
    - ./:/app
    - /app/node_modules

volumes:
  postgres_data:
networks:
  sig-shared-services:
    external: true
  sig-operafr-network:
    driver: bridge
